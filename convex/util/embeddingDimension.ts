// Convex evaluates vector index dimensions while compiling the schema, where
// runtime environment variables are intentionally unavailable. Keep this value
// static and aligned with the default local mxbai-embed-large model.
export const EMBEDDING_DIMENSION = 1024;
